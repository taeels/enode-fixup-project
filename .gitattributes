# 이어 붙이는 AI-DLC 감사 로그는 병합에서 양쪽 줄을 다 살린다.
# 문서 루트가 소유자마다 갈리므로(회차는 aidlc-docs/<브랜치>/ · Construction 은
# aidlc-docs/<handle>/) 보통은 안 부딪치지만, 같은 경로를 두 손이 이어 붙이면
# git 이 합치게 한다.
aidlc-docs/**/audit.md merge=union
