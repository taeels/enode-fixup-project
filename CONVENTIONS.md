# 이 저장소의 규약

정본 설계는 `enode-design` 에 있다. 여기는 그 구현이고, 아래 둘은 이 저장소
안에서만 적용된다.

---

# 1. 표기 — 강조는 마크다운 굵게로만 한다

`enode-design/CLAUDE.md` 의 표기 규약 제2판을 그대로 따른다. 실패 분석과
근거는 `enode-design/docs/notation-2nd-edition.md` 에 있다.

## 1.1 강조 수단은 하나다

마크다운이 렌더링되는 자리(README · 문서)에서는 `**굵게**` 만 쓴다.
특수문자로 어절을 감싸서 강조하지 않는다.

## 1.2 코드에는 장식 문자를 넣지 않는다

주석 · 문자열 · 로그 · 테스트 메시지 · 프롬프트 · 설정 파일 어디에도 넣지
않는다. Go 의 주석에는 강조 문법이 없으므로 장식 문자는 강조가 아니라
그냥 글자다.

```text
   강조하고 싶으면    문장을 짧게 끊고 앞으로 옮긴다
                      주석의 첫 줄이 이미 가장 강한 자리다
```

## 1.3 커밋 메시지에도 넣지 않는다

`git log` 는 마크다운을 렌더링하지 않는다. 제목은 무엇을 했는지로만 채운다.

## 1.4 세는 것으로 지킨다

`enode-design/scripts/emphasis-check.py` 가 밀도와 뭉침을 센다. 도구는 두
벌로 두지 않는다 — 두 벌이 되면 규칙이 갈린다.

---

# 2. 언어 — 코드는 영어로 쓴다

## 2.1 영어로 쓰는 자리

```text
   에러 문자열           호출자와 로그가 읽는다
   로그 메시지
   CLI 출력
   테스트 이름과 실패 메시지
   테스트 픽스처 문자열
   에이전트 프롬프트     하네스에 실려 나간다
```

## 2.2 한국어로 남기는 자리

```text
   주석 (godoc 포함)     설계 논거가 그대로 들어 있다. 옮기면 뜻이 얇아지고
                         분량이 4,555줄이라 옮기는 동안 원본과 갈린다
   커밋 메시지           사람이 읽는다. 설계 논의와 같은 언어를 쓴다
   testdata 안의 기록    실제로 보냈던 것의 기록이다. 고치면 기록이 아니다
```

주석이 예외인 이유는 되먹임 경로에 있는지로 갈린다. 주석은 하네스로 실려
나가지 않는다. 프롬프트와 출력은 나간다.

## 2.3 왜 가르는가

이 저장소는 에이전트를 돌리는 제품이다. 프롬프트가 한국어면 **제품이 실행
중에 그 언어와 표기를 에이전트에게 다시 주입한다.** 에이전트가 내는 계획과
보고서가 같은 형태로 돌아오고, 그것이 다시 저장소에 쌓인다. 언어를 가르는
것은 취향이 아니라 그 되먹임을 끊는 일이다.

그래서 기준은 「코드냐 아니냐」가 아니라 **「밖으로 나가느냐」**다.

---

# 3. 커밋 — 토픽 브랜치 위에서 돈다

1절이 커밋 메시지의 표기를, 2.2절이 그 언어를 정한다. 여기는 **언제 · 어디에 ·
누가 · 무엇을** 이다. 이 절이 그 값의 정본이다.

## 3.1 어디에

```text
   실행 브랜치    v<N>-run-<이름>      Inception 이 여기서 직렬로 돈다.  main 에서 딴다
   유닛 브랜치    unit/<유닛>          Construction.  회차 브랜치에서 딴다
```

**둘 다 PR 로 `main` 에 모인다.** 회차 브랜치는 Inception 이 닫히면 올린다 —
유닛 정의가 거기 있고 다음 회차가 그것을 딛는다. 유닛 브랜치는 그 유닛의 장면
게이트가 초록인 뒤에 올린다.

대회 때 여기 「유닛이 회차 브랜치로 모인다」고 적었다. **실제로는 안 그랬다** —
유닛 PR 스물여섯이 전부 `main` 으로 갔고, 회차의 Inception 커밋은 첫 유닛 PR 에
실려 `main` 에 닿았다. 그 경로는 회차에 Construction 이 없으면 끊긴다. 그래서
회차 브랜치를 따로 올리는 것으로 고쳤다.

**병합 순서는 의존 그래프가 정한다.** 팩은 유닛 분해를 주지 않으므로
(`constraints.md` 구조 불변식) 순서도 미리 못 박지 않는다 — Units Generation 이
낸 의존을 보고 의존 없는 유닛부터 병렬로 착수한다.

`internal/api/api.go` 의 등록 줄처럼 **유닛 둘 이상이 만지는 파일**은 파일
행렬에 표시하고 진행자가 직렬로 병합한다. 한 번에 하나다.

## 3.2 누가

**에이전트가 커밋한다.** 3.1 의 두 브랜치 위에서 돈다.

**레이어링이 부딪히는 자리를 셋에서 하나로 줄였다.** 상태와 감사가 소유자마다
갈리기 때문이다 — 회차는 회차(브랜치) 이름으로, Construction 은 담당 handle 로
(`CLAUDE.md` 의 문서 루트 규약).

```text
   aidlc-docs/<회차>/aidlc-state.md      그 회차의 진행자가 고친다
   aidlc-docs/<handle>/aidlc-state.md    그 담당이 고친다
   aidlc-docs/**/audit.md                이어 붙인다.  .gitattributes 의 merge=union
   design/                               남는 하나.  진행자만 고친다.  pen 을 안 건드린다
```

유닛의 기록은 그 담당의 유닛 폴더(`aidlc-docs/<handle>/construction/<유닛>/**`)에
적는다. **소유자가 하나라 병합에서 안 부딪친다.** 대회 때는 상태와 감사가 한 장씩이라
진행자가 병합 뒤 옮겨야 했고, 그 옮기는 경로를 레이어링이 없앴다.

## 3.3 언제

**커밋 지점과 병합 지점이 다르다.**

```text
   커밋한다    단계 승인마다.  Inception 의 각 단계 · 유닛의 각 단계
               산출물이 나왔으면 그 자리에서 역사에 넣는다

   병합한다    그 유닛의 장면 게이트가 초록이 된 뒤.  unit/<유닛> -> main (PR)
               빨간 채로 병합하면 다음 사람이 서지 않는 나무를 받는다
```

게이트가 유닛 밖의 이유로 빨가면 `scene-gates.md` §4 의 보류로 적고 넘어간다.
**보류는 통과가 아니므로 병합 지점도 아니다.** 커밋은 그대로 쌓는다 — 브랜치가
따로 있으므로 남아 있어도 남을 방해하지 않는다.

## 3.4 무엇을

한 커밋은 **그 단계의 산출물** 또는 **그 유닛의 코드와 문서**다.

```text
   싣는 것    그 단계 · 그 유닛이 만든 파일과 고친 파일
              aidlc-docs/<handle>/construction/plans/<유닛>-*
              aidlc-docs/<handle>/construction/<유닛>/**
              자기 문서 루트의 aidlc-state.md 와 audit.md

   안 싣는 것  design/           진행자가 고친다.  3.2 의 남는 하나
              남의 문서 루트     다른 회차 · 다른 handle 의 상태와 감사
```

**자기 상태 파일을 자기가 싣는다.** 대회 때는 안 실었다 — 한 장을 모두가 만졌기
때문이다. 레이어링 뒤에는 그 파일의 소유자가 하나라 미룰 이유가 없다.

## 3.5 끝나면

```bash
git push -u origin unit/<유닛>        # 유닛마다
git push -u origin v<N>-run-<이름>    # 회차마다.  둘 다 PR 로 main 에 간다
```

가져갈 것 — 유닛 산출물 · 질문 파일(몇 개를 물었나) · 조각마다의 시각 ·
행렬 밖 파일을 만진 diff · 유닛이 실제로 병렬로 돌았는지의 기록.
