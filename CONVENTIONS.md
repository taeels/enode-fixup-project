# 이 저장소의 규약

정본 설계는 `enode-design` 에 있다. 여기는 그 구현이고, 아래 둘은 이 저장소
안에서만 적용된다.

---

# 1. 표기 — 강조는 마크다운 굵게로만 한다

`enode-design/CLAUDE.md` 의 표기 규약 제2판을 그대로 따른다. 실패 분석과
근거는 `enode-design/docs/notation-2nd-edition.md` 에 있다.

## 1.1 강조 수단은 하나다

마크다운이 렌더링되는 자리(README · 문서)에서는 `**굵게**` 만 쓴다.
특수문자로 어절을 감싸서 강조하지 않는다.

## 1.2 코드에는 장식 문자를 넣지 않는다

주석 · 문자열 · 로그 · 테스트 메시지 · 프롬프트 · 설정 파일 어디에도 넣지
않는다. Go 의 주석에는 강조 문법이 없으므로 장식 문자는 강조가 아니라
그냥 글자다.

```text
   강조하고 싶으면    문장을 짧게 끊고 앞으로 옮긴다
                      주석의 첫 줄이 이미 가장 강한 자리다
```

## 1.3 커밋 메시지에도 넣지 않는다

`git log` 는 마크다운을 렌더링하지 않는다. 제목은 무엇을 했는지로만 채운다.

## 1.4 세는 것으로 지킨다

`enode-design/scripts/emphasis-check.py` 가 밀도와 뭉침을 센다. 도구는 두
벌로 두지 않는다 — 두 벌이 되면 규칙이 갈린다.

---

# 2. 언어 — 코드는 영어로 쓴다

## 2.1 영어로 쓰는 자리

```text
   에러 문자열           호출자와 로그가 읽는다
   로그 메시지
   CLI 출력
   테스트 이름과 실패 메시지
   테스트 픽스처 문자열
   에이전트 프롬프트     하네스에 실려 나간다
```

## 2.2 한국어로 남기는 자리

```text
   주석 (godoc 포함)     설계 논거가 그대로 들어 있다. 옮기면 뜻이 얇아지고
                         분량이 4,555줄이라 옮기는 동안 원본과 갈린다
   커밋 메시지           사람이 읽는다. 설계 논의와 같은 언어를 쓴다
   testdata 안의 기록    실제로 보냈던 것의 기록이다. 고치면 기록이 아니다
```

주석이 예외인 이유는 되먹임 경로에 있는지로 갈린다. 주석은 하네스로 실려
나가지 않는다. 프롬프트와 출력은 나간다.

## 2.3 왜 가르는가

이 저장소는 에이전트를 돌리는 제품이다. 프롬프트가 한국어면 **제품이 실행
중에 그 언어와 표기를 에이전트에게 다시 주입한다.** 에이전트가 내는 계획과
보고서가 같은 형태로 돌아오고, 그것이 다시 저장소에 쌓인다. 언어를 가르는
것은 취향이 아니라 그 되먹임을 끊는 일이다.

그래서 기준은 「코드냐 아니냐」가 아니라 **「밖으로 나가느냐」**다.

---

# 3. 커밋 — 유닛 하나가 커밋 하나다

1절이 커밋 메시지의 표기를, 2.2절이 그 언어를 정한다. 여기는 **언제 · 어디에 ·
누가 · 무엇을** 이다. 이 절이 그 값의 정본이다.

## 3.1 어디에

```text
   실행 브랜치    v1-run-<이름>       Inception 이 여기서 돈다
   유닛 브랜치    unit/<유닛>          Construction 이 여기서 돈다
   main          손대지 않는다        다음 사람이 처음부터 다시 할 수 있게
                                      스캐폴드 상태로 둔다
```

병합 순서는 착수 순서다 — 바닥 · 관측 · 대기열 · drain · 제어판.
`internal/api/api.go` 의 등록 줄은 직렬로 받는다. 한 번에 하나다.

## 3.2 누가

**진행자만 커밋한다.** 에이전트는 파일을 내고 게이트를 돌리고 거기서 멈춘다.

이유는 취향이 아니다. `aidlc-docs/audit.md` 와 `aidlc-docs/aidlc-state.md` 가
각각 **한 장**이고 모든 유닛이 같은 파일에 쓴다. 유닛 브랜치가 각자 커밋하면
병합에서 부딪친다. `design/` 도 같은 이유로 진행자만 고친다 —
유닛 브랜치는 pen 을 안 건드린다.

**AI-DLC 단계 승인은 커밋 승인이 아니다.** 「Continue to Next Stage」는 산출물을
받았다는 뜻이지 그것을 역사에 넣으라는 뜻이 아니다. 커밋은 따로 말해야 한다.

## 3.3 언제

**그 유닛의 장면 게이트가 초록이 된 뒤다.** CP0 이 빨간 채로 U-바닥을 커밋하면
다음 사람이 그 지점으로 되돌아가도 서지 않는 나무를 받는다.

```text
   커밋 지점      CP0 초록 · CP1 초록 · CP2 초록 · CP3 초록 · CP4 초록
   커밋 아닌 것    단계 승인 · 계획 승인 · 설계 승인
```

게이트가 유닛 밖의 이유로 빨가면 `scene-gates.md` §4 의 보류로 적고 넘어간다.
**보류는 통과가 아니므로 커밋 지점도 아니다.**

## 3.4 무엇을

한 커밋은 **그 유닛의 코드와 그 유닛의 문서**다.

```text
   싣는 것    그 유닛이 만든 파일과 고친 파일
              aidlc-docs/construction/plans/<유닛>-*
              aidlc-docs/construction/<유닛>/**

   미루는 것   aidlc-docs/aidlc-state.md      유닛마다 다시 쓰인다
              aidlc-docs/audit.md            유닛마다 이어 붙는다
```

**상태 파일 둘은 마지막 커밋에 싣는다.** 유닛마다 통째로 다시 쓰이는 파일이라
중간 판이 남아 있지 않고, 없는 판을 지어내면 그것은 기록이 아니다. 유닛별
사실은 그 유닛의 요약 파일이 이미 지고 있다.

## 3.5 끝나면

```bash
git add -A && git commit -m "v1 실행 — <이름>"
git push -u origin v1-run-<이름>
```

가져갈 것 — 유닛 산출물 · 질문 파일(몇 개를 물었나) · CP0 ~ CP4 의 시각 ·
행렬 밖 파일을 만진 diff.
