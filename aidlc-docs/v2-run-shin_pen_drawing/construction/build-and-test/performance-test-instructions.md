# 성능 검사 — 해당 없음

산출물이 시안이라 응답 시간 · 처리량 · 동시 사용자의 대상이 없다.
NFR Requirements 를 실행 계획에서 건너뛴 것과 같은 이유다.

pen.dev 파일 자체의 크기만 적어 둔다 — `enode-demo.pen` 약 850 KB. 원본
`enode-ux.pen`(410 KB)을 복사해 화면 여덟을 지우고 아트보드 여덟을 더한
결과다. 열고 내보내는 데 문제 없다.
