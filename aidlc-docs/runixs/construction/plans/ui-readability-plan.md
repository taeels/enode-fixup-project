# 함대 탐색과 정보 가독성 보완 — 2026-09-09

공식 단계는 승인된 CONSTRUCTION Code Generation이다. 담당 roster와 기존
audit의 사용자 handle runixs를 복원했다. 이번 사용자 요청을 기존 UI 계획
1·2·5·10단계의 보완 지시로 반영한다. 새 Inception이나 승인된 FD를 반복하지 않는다.
작업 브랜치는 `unit/runixs-ui-readability`, 시작점은 `a5d62cf`다.

## 수용 조건

| 요청 | 변경과 확인 |
|---|---|
| 스크롤 확대·축소 | 함대와 그래프의 2D/3D에서 휠로 포인터 주변을 확대·축소한다. 15~300% 범위, Fit/100%와 보기별 위치 복원을 유지한다. 정보 카드·목록 내부 스크롤에는 영향을 주지 않는다. |
| 작업 그래프 구분·복귀 | 장치 모형 대신 순번·상태·실행 노드가 있는 단계 카드와 의존 화살표를 사용한다. 3D는 카드의 깊이와 기존 배치를 유지한다. 작업 그래프 제목·선택 작업·제출자와 함대로 돌아가기 버튼을 제공한다. |
| 임대 대상 | 현재 `node.lease.run_id`에 일치하는 `runs[].submitter`만 연결한다. 함대 카드와 상세에 표시하며 목록에 없거나 빈 이름이면 제출자 미확인/미제공으로 표시한다. 과거 assigned에서 현재 임대를 추정하지 않는다. |
| 사람이 읽는 상세 | 기능별 이름·속성 표, 알려진 키의 한국어 설명, 읽기 쉬운 날짜와 남은 시간을 제공한다. 노드/인스턴스 ID·경로·원문 광고는 접힌 기술 상세에 보존한다. sandbox는 기존 능력별 광고만 표시한다. |

## 실행

- [x] 1. 기존 승인·API 소비 계약·소유 경계를 확인하고 보완 설계를 기록한다.
- [x] 2. `internal/api/ui/static/shared/fleet/`의 표시 보조 모듈, view·scene·CSS를 구현한다.
- [x] 3. 시간·임대 연결 회귀를 Node로, 휠·복귀·정보 읽기·좁은 화면·폴링을 브라우저로 검증한다. Go UI 테스트·vet·표기 검사·빌드로 embed를 확인한다.
  Node 51, 합성 관측 브라우저 45개 단언, Go UI 98.4%, vet·glyphscan·Mediator 빌드 통과.
- [x] 4. 구현·검증 결과와 담당 state/audit를 갱신한다.
  [결과 기록](../ui/code/ui-readability.md). 전체 유닛 단계·공동 CP 보류는 유지한다.

새 API·DB·라이브러리는 필요 없다. 기존 obs·queue·drain 코드는 현재 체크아웃에
있으며 이전 인수 기록을 계승한다. 이 보완 검증은 실제 LED·음원·방송을 포함한
공동 CP10 통과를 의미하지 않는다. 중지한 실제 런타임은 재기동하지 않는다.

## 보안

SECURITY-04·05·08·11·12·13·15: 기존 CSP·인증·검증을 유지하고 모든 광고값과
제출자를 텍스트로 삽입한다. HTML 형태 입력·미확인 값·오래된 관측을 검증한다.
SECURITY-01·03·07: decisions §3의 기존 코드 예외를 상속한다.
SECURITY-02·06·09·10·14: 네트워크 중개·권한·배포·의존·운영 변경이 없어 N/A.
resiliency-baseline·property-based-testing은 기존 Disabled를 유지한다.


## 브라우저 확인 후 추가 피드백

- [x] 5. 함대 노드 간격을 넓히고 긴 이름은 두 줄로 표시한다. 긴 영문 호스트명·한글 이름과 최대 길이 제출자로 2D/3D에서 텍스트 겹침·카드 넘침·전체 이름 접근성을 검증한다.
- [ ] 6. 사용자 요청에 따라 미리보기 탭을 열어두고 변경을 커밋·push·PR로 제출한다. 기존 공동 CP 보류와 병합 전 상태는 유지한다.
