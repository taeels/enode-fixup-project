# UI 개선 변경 계획 — 2026-09-09

사용자 요청의 세 개선을 승인된 UI 코드 생성 계획의 보완으로 반영한다.
공식 단계는 CONSTRUCTION → Code Generation이다. Inception·공용 pen·다른
유닛의 요구를 다시 쓰지 않는다. queue@310c22d 인수 결과를 유지한다.

## 변경과 수용 조건

| 요청 | 구현과 수용 조건 |
|---|---|
| 웹캠뷰 닫기 | 닫기 버튼·접근 가능한 이름. 닫으면 iframe·설정 요청을 정리하여 재생을 멈춘다. 헤더의 웹캠 버튼으로 다시 열고 크기·확대·이동 위치는 페이지 안에서 유지한다. 투어를 다시 시작하면 웹캠을 열어 첫 대상을 표시한다. |
| 한글 두 단어 이름 | `차분한 수달` 형태, 완성형 한글 두 단어와 공백 하나. 기존 Guest 저장 키·온보딩 키 유지. 기존 영문 Guest 이름은 결정적으로 한글 이름으로 옮기며 새로고침에도 같다. 신규 제출은 한글 이름. 이전 미확인 요청의 세 필드는 이름을 포함해 그대로 복구·재시도한다. 과거 서버 기록은 바꾸지 않는다. |
| 플로팅 상세 자동 닫기 | 노드·단계 상세는 바깥 클릭, 배경 스크롤·키보드 이동, 보기/Run/필터 전환 시 닫는다. 내부 읽기·스크롤·폴링은 유지하고 다른 카드는 즉시 새 상세로 바뀐다. 새 작업 모달·투어의 바깥 클릭도 닫되 제출 요청과 재시도 정보는 취소하지 않는다. |

## 순서

- [x] **1 — 변경 영향과 설계 반영**: 이 계획과 기존 FD/제출 계약을 갱신한다.
  사용자 “위와같은 개선사항 요청이 왔어. aidlc에 반영해서 진행하자”를 이 변경의
  구현 지시로 기록한다. 인증·API 저장·queue 유닛은 그대로 소비한다.
- [x] **2 — 구현**: `internal/api/ui/static/shared/guest.js`,
  `shared/fleet/view.mjs`, `demo/webcam.mjs`, `demo/demo.js`,
  `demo/submission.mjs`, `demo/tour.mjs`, 필요한 demo CSS를 수정한다.
- [x] **3 — 검증**: Guest 이름 이전·저장 실패·한글 제출·이전 요청 재시도는
  Node 테스트로 확인한다. 실제 브라우저에서 닫기/다시 열기/투어/포커스·상세
  내부 조작/바깥 조작/다른 카드·모달 배경 닫기를 검증한다. 기존 Node·Go UI 검사 실행.
- [x] **4 — 기록**: 코드 요약·검증 결과·담당 state/audit를 갱신한다.
  queue 인수와 독립 UI 개선을 완료해도 실제 LED/음원·방송 CP9/10/11은 보류다.

## 제출 계약 변경

신규 submitter는 `^[가-힣]{1,12} [가-힣]{1,12}$`, 최대 UTF-8 73바이트다.
서버는 기존 `^guest-[a-z]{1,24}-[a-z]{1,24}$`도 이전 요청 재시도 호환을 위해
허용한다. 원문을 정규화·번역해 run_id를 다시 만들지 않는다. scenario_id·request_id,
SHA-256 식별 계산, 4 KiB 상한과 입력 세 필드는 동일하다. backend가 아직 없으므로
실제 라우트 구현은 진행자 픽스처 인수 뒤 이 계약을 적용한다.


**커밋·push 승인**: 2026-09-09T01:25:46.761181+00:00 “그러면 일단 commit해서 push할까”.
이 보완과 queue 인수 기록을 담당 브랜치에 저장·공유한다. 실제 시나리오 인수와
공동 게이트의 보류는 유지한다.
