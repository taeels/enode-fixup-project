# Drain 인수와 접속 보완

2026-09-09 사용자의 최신 main 인수·추가 작업·접속 확인 지시를 기존 UI 관측과
진입 동작의 보완으로 실행한다. 별도 Inception·FD를 다시 시작하지 않는다.
drain 제어 구현은 shin-son, 제어판은 nacl1119의 범위이며 여기서는 기존
UI가 실제 정책·경계 취소·queue 승격을 정확히 관측하는지 확인한다.

- [x] 1. 최신 병합 확인과 인수. PR #6·card-news를 666126a에서 인수하고,
  사용자 후속 안내 후 PR #7/main 50af6cf를 fast-forward했다. 영상과 미커밋 audit 보존.
- [x] 2. 기본 주소 접속 보완. 정확한 GET/HEAD /만 /ui/로 이동한다.
  잘못된 API 경로 404와 쓰기 인증을 유지하는 회귀 검사를 추가한다.
- [ ] 3. 합친 코드의 API·enode·store·UI 검사와 빌드를 실행한다.
  DB 검사는 별도 DB와 scripts/testdb.sh를 사용한다. 변경 없는 유닛의 설계를 다시 쓰지 않는다.
- [ ] 4. 현재 로컬 환경을 새 구현으로 갱신한다. 기존 DB·토큰·노드 신원을 유지하고
  버전은 rc1과 구분한다. 실제 두 단계 shell 작업에서 at-boundary·graceful,
  경계 취소 사유·봉인·queue 승격과 UI의 진행 중/대기 중 표시를 검증한다.
- [ ] 5. LAN의 기본 주소·UI·API 응답과 방화벽 허용을 확인하고 결과를 기록한다.
  다른 기기의 접속 성공은 그 기기의 결과가 있을 때만 확정한다.
- [ ] 6. 담당 상태·인수 기록을 갱신하고 지정 이메일로 변경을 커밋·push한다.
  기존 PR #6은 병합됐으므로 후속 변경은 별도 PR로 낸다. rc1 태그/자산은 변경하지 않는다.
