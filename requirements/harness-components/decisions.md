# 결정표 — 이미 정해진 것. 다시 논의하지 않는다

이 표는 `features.md` 의 미정을 **착수 전에 값으로 닫은 것**이다. 게이트에서는
확인만 한다. 새로 묻지 않는다.

**게이트 규칙 한 줄 — 권장을 벗어나면 근거를 적는다.** 벗어나는 것은
허용이다. 근거 없이 벗어나는 것이 금지다.

```text
   1절   사용자가 확정한 것          값이다.  게이트에서 묻지 않는다
   2절   팩이 채운 권장값            값이다.  벗어나면 근거를 적는다
   3절   보안 확장 아래서의 취급      SECURITY 규칙마다 처리를 미리 적었다
   4절   이월                        이번에 정하지 않는다
   5절   정본에 되돌려 올리는 것      이 회차가 끝나면 진행자가 올린다
```

정본과 어긋나면 `enode-design/protocol/INVARIANTS.md` 가 이긴다 (`canon.md`).

```text
   정본이 이미 지는 값 — 팩이 고를 자리가 아니다

     매칭 기준 「부칠 수 있는가」          ADR-035 §2
     부칠 수 있는 것은 요구에 안 넣는다     ADR-035 §4.1
     MCP 탐지는 「설정에 있나」가 아니라 「뜨나」  ADR-035 §4.4
     키를 늘리고 매처는 안 고친다          ADR-035 §4.3
     팩은 blob 이다                       ADR-034 §2.2
     Fixed 는 Instrument 와 독립이다        ADR-034 §2.1
     계정 커넥터는 막고 Run 이 준 것은 연다  ADR-067 §2.1 (초안 — 5절이 결정으로 올린다)
```

---

# 1. 사용자가 확정한 것 (2026-09-11)

| 항목 | 값 | 근거 |
|---|---|---|
| 범위 | 사내 실측 후속을 팩 둘로 가른다. 이 팩은 하네스 구성요소. 다른 팩은 `requirements/transcript/` | 축이 둘이고 독립이다. 같은 팩에 두면 게이트와 정본 수정이 섞인다 |
| 사내 MCP 를 하네스에 물린다 | 하네스 환경에 설치된 MCP 가 거부되던 것을, 노드가 선언하고 계약이 요청하면 물리게 한다 | 사내 실측. `ADR-035` §7 의 여는 조건 「부칠 수 없는 MCP 를 실제로 쓰게 될 때」가 충족됐다 |
| 전달 방향 | `runctl` 로 계약을 내는 사람과 오케스트레이터가 하네스 구성요소(스킬 · MCP 정의)를 실어 보낼 수 있어야 한다 | 사용자 진술. 정본은 `ADR-034` 가 문법까지 정했고 코드가 0 이었다 |
| 확장 opt-in | **security-baseline 켬.** resiliency-baseline · property-based-testing 끔 | 앞 회차의 선택을 그대로 잇는다. 취급은 3절 |

---

# 2. 팩이 채운 권장값

| 항목 | 권장값 | 근거 |
|---|---|---|
| 홈 변수 | `CLAUDE_CONFIG_DIR=<계장 임시 디렉터리>/home`. `Fixed()` 가 값으로 박는다. `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` 은 그대로 둔다 | `ADR-034` §2 · §2.1. 실측 D — 가짜 홈이 계정 커넥터와 개인 서버를 끊는다 |
| `--setting-sources ""` | 유지한다 | 실측 B · E — 이것이 개인 · 프로젝트 설정과 `.mcp.json` 을 끊는 층이다. 가짜 홈과 끊는 것이 다르다 |
| OAuth 자격증명 | 노드의 `~/.claude/.credentials.json` 이 있으면 가짜 홈에 복사한다. `0600` · 계장 임시 디렉터리와 함께 단계 끝에 삭제. 없으면 아무것도 안 한다 (환경변수 · 게이트웨이 노드) | `ADR-034` §3 ① 실측 — 홈을 옮기면 `Not logged in`. transparent 인증(R1 이 `HOME` 을 통과시키는 뜻)을 유지한다. 복사본이 임시 디렉터리 밖으로 안 나가고 같은 기계에 머문다. `Usable()` 은 실제 홈으로 재는 오늘 그대로 — 같은 파일이므로 광고와 실행이 어긋나지 않는다 |
| 허용목록 파일 | `<계장 임시 디렉터리>/mcp.json`. `{"mcpServers": {...}}` 모양. 플래그는 `--strict-mcp-config --mcp-config=<경로>` 등호 형태. 요청이 없으면 빈 목록 | `ADR-067` §2.1 이 기계를 지목했다. 등호는 `ADR-034` §3 ② 의 가변인자 함정 회피. 실측 C — strict 단독은 무변화라 파일과 함께 넘긴다 |
| 허용목록의 내용 | **6절 ⑥ ⑦ 이 뒤집었다 — 아래는 낡은 값이다.** ~~팩의 `mcp.json` 전부~~ + 노드 `mcp:` 중 `agent.mcp` 가 적은 것 + 워크스페이스 `.mcp.json` 중 `agent.mcp` 가 적은 것. 이름이 겹치면 팩 · 노드 · 워크스페이스 순으로 앞이 이기고 로그에 남긴다 | 세 출처가 있고 계약이 고른다. 팩이 이기는 이유 — 계약이 실어 보낸 것이 가장 재현 가능하다. **지금 값은 6절 ⑥(셋 다 `agent.mcp` 필터를 탄다)과 ⑦(노드 선언 이름을 팩이 덮으면 거절)이다** |
| 요청한 이름이 없을 때 | 하네스를 안 띄우고 단계를 실패로 보고한다. 사유 `mcp server <이름> is not available on this node` | `ADR-035` §3. 조용히 빼면 exit 0 으로 성공이 봉인된다 |
| 계정 커넥터 | 언제나 0. 여는 값이 없다 | `ADR-067` §2 — 노드를 나간다. 가짜 홈과 strict 가 두 겹으로 막는다. **첫 겹은 로그아웃 홈에서만 쟀다** — FR-1 이 `.credentials.json` 을 복사하므로 CA1 이 로그인된 홈에서 다시 잰다 (`application-design.md` 4.6) |
| 노드 선언 형식 | `enode.yaml` 의 `mcp:` 맵. `command` + `args` (stdio) 또는 `url` + `credential` (remote). 그 밖의 키는 그대로 허용목록에 옮긴다. **아는 키의 모양은 검증한다** — 틀리면 노드가 안 뜬다 (US-2 · U3 완료 조건) | `ADR-035` §4.4 의 모양 그대로. 하네스가 읽는 형식과 같아 옮겨 적기가 복사다 |
| 뜨나 | stdio — `command` 가 절대경로로 있거나 `PATH` 에서 resolve 된다. remote — `credential` 이름의 환경변수가 노드 환경에 있다. 연결은 안 한다 | `ADR-035` §4.4. 연결 확인은 광고마다 남의 서버를 두드린다 — 순연 |
| 탐지 자리와 주기 | 비싼 탐지(주기 장치 `Detector`, 기본 5분). **6절 ⑤ 가 이름과 종류 수를 고쳤다** — 인터페이스 이름은 `Fingerprinter` 이고(`Detector` 는 `ADR-068` 의 주기 장치가 쓴다) 종류는 하네스 · 저장소 · MCP **셋**이다 | `ADR-035` §4.2 · `ADR-068`. 실행파일 resolve 는 프로세스를 안 띄우지만 호출 수가 노드 설정에 비례하므로 값싼 쪽에 안 둔다 |
| 광고 키 | `mcp.<이름>: "1"` · `harness.<이름>: "1"`. 옛 `harness: <이름>` 은 한동안 같이 싣는다 — **걷는 시점은 이 회차가 안 정한다** (걷는 날 매처의 attrCount 가 노드마다 1 씩 줄어 정렬이 움직인다). 값은 언제나 `"1"` | `ADR-035` §4.3 · §6. 매처 무변경. 버전은 안 싣는다 (동등 비교뿐) |
| 자격증명 이름의 노출 | 광고에 `mcp.<이름>` 만 실린다. `credential` 의 환경변수 이름은 광고에 안 싣는다. 값은 어디에도 안 싣는다 | `ADR-035` §8 의 질문을 값으로 닫는다. 「그 기계가 Gerrit 자격을 가졌다」는 `mcp.gerrit` 으로 이미 드러나고, 변수 이름까지 실을 이유가 없다 |
| 워크스페이스 `.mcp.json` | 실행 시점에 읽어 요청된 이름만 허용목록으로 옮긴다. 광고하지 않는다. `project` 소스는 켜지 않는다 | `features.md` 3.4. 실측 F2 — project 소스를 켜면 저장소 설정이 통째로 딸려온다 |
| 워크스페이스 `.claude/skills/` | 하네스가 스스로 읽는지 CA5 가 잰다. 안 읽으면 팩으로 나른다 | 값이 아니라 실측 항목이다. 5절이 아니라 여기 두는 이유는 어느 쪽이든 문법이 안 바뀌기 때문이다 |
| 계약 문법 | `requires[].mcp.<이름>: "1"` (있는 문법) · `agent.mcp: [이름]` · `agent.pack: <blob 이름>` + `in.from` | `ADR-013` 결정 4 — 매칭과 실행을 가른다. `ADR-034` §2.2 |
| `agent` 의 알려진 키 | `model` · `max_turns` · `max_tokens` · `ask` · `harness` · `mcp` · `pack`. 그 밖은 `400` | `ADR-034` §5 ④. 오타가 조용히 사라지는 것을 막는다. 검증 자리는 `contract.Validate` — Mediator 와 enode 가 같은 패키지를 본다 |
| 팩의 형식 | tar 하나. 안에 `skills/<이름>/SKILL.md` · `agents/<이름>.md` · `mcp.json`. 그 밖의 파일은 무시하고 로그에 남긴다 | `ADR-034` §1.1 — SKILL.md 디렉터리 규약이 다섯 하네스에 공통이다. tar 인 이유는 blob 이 파일 하나이기 때문이다 |
| 팩의 출처 | 계약의 명령 단계가 `$OUT/pack` 에 tar 를 낸다 (`git archive --remote` · `curl -o` 등 argv 하나). `agent.pack` 이 그 이름을 적고 `in.from` 이 잇는다 | `ADR-034` §2.2 「새 부품 0 개」. 계약 인라인은 기각됐고 노드에 미리 두는 것도 기각됐다 |
| 오케스트레이터 | `cmd/iapadapter` 설정에 `executor.pack: { fetch: [argv], name: pack }`. 비면 오늘 그대로. 템플릿이 팩 단계와 `agent.pack` 을 더한다 | 사용자 확정 「전달 방향」. 계획이 짓는 단계는 문법 안내가 `agent.mcp` · `agent.pack` 을 알려주고 안 적으면 팩 없이 돈다 |
| 기록 | `HarnessResult` 에 `mcp: [이름]` 과 `pack: <sha256>` | `ADR-034` §5 팩 다이제스트 자리. `ADR-005` 성질 4 |
| 검증 수단 | `claude mcp list` 를 게이트에 쓰지 않는다. `-p --output-format stream-json --verbose` 의 `system/init` 줄로 잰다 | 실측 C · G — `mcp list` 는 strict 와 `--mcp-config` 를 반영하지 않는다 |

---

# 3. 보안 확장 아래서의 취급

`security-baseline` 이 켜져 있다. 규칙은 **차단성**이다. 규칙마다 처리를 미리 적는다.

```text
   SECURITY-01  전송 암호화     기존 코드 사실로 기록만.  수정 안 함
   SECURITY-03  로깅            기존 코드 사실로 기록만.  새 로그는 이름만 찍는다 —
                               빠진 MCP 의 이름 · 겹친 이름.  값은 안 찍는다
   SECURITY-07  네트워크        기존 코드 사실로 기록만.  새 포트 0
   SECURITY-08  접근 제어       새 표면에 적용.  허용목록이 곧 접근 제어다 —
                               계약이 요청하지 않은 서버 0 · 계정 커넥터 0.
                               노드 선언은 소유자만 쓰는 설정 파일에 산다 (ADR-063)
   SECURITY-12  인증 · 자격증명  N/A.  그 규칙은 사용자 인증(비밀번호 정책 · 적응형
                               해싱 · MFA · 세션 쿠키 · 무차별 대입 · 하드코딩)이고
                               계장의 자격증명 파일 복사는 거기 안 걸린다.
                               복사본의 0600 · 단계 끝 삭제 · 임시 디렉터리 밖으로
                               안 나감은 3.1 의 보안 요구가 받는다.
                               허용목록과 광고에는 이름만, 값은 없다.
                               R1 화이트리스트는 그대로다 — ENODE_TOKEN 은 안 넘어간다
```

**하지 말 것** — 보안 이유로 R1 화이트리스트를 넓히는 것. 계정 커넥터를 여는
값을 두는 것. 팩의 `settings.json` 을 읽는 것 (훅 설정이 정책인데 팩이 그것을
덮는다).

---

# 4. 이월 — 이번에 정하지 않는다

```text
   runctl submit --pack           막고 있는 값 — 매칭 전 seq 0 blob 쓰기의 API 자리
   max_tokens -> max_budget_usd    ADR-034 §4.  이 팩과 무관해서 뺐다.  알려진 키 검증이
                                  max_tokens 를 거절하지 않는다
   자격증명 주입 (R2)              Credentials 자리 그대로.  「누구의 자격으로 도나」는 별개다
   도구 이름 차단                  ADR-067 §3 순연 그대로
   tools/list 속성 매핑            ADR-012.  붙일 실물이 생기면
   팩 캐시 · system.md 교체        ADR-034 §5 · §7
   워크스페이스 항목의 자동 광고     노드 소유자가 mcp: 로 옮겨 적는 것이 유일한 길이다
```

---

# 5. 정본에 되돌려 올리는 것

이 회차가 끝나면 진행자가 `enode-design` 에 올린다. 서브모듈은 진행자만 올린다.

```text
   ADR-067    초안 -> 결정.  §5 미결 둘의 답 —
              정책은 노드 설정(mcp:)과 계약 요청(agent.mcp)의 교집합에 산다.
              Run 이 MCP 를 요구하는 문법은 requires 의 mcp.<이름> 과 agent.mcp 다
   ADR-034    §5 「오늘 하는 것」 ①② 착수 기록.  §3 ① 의 OAuth 파일 복사 값.
              팩 tar 규약과 팩 단계.
              §3 ② 정정 — 「가변인자다 ⇒ 쓰지 않는다. MCP 는 홈의 파일로만 준다」가
              뒤집혔다.  등호 형태(--mcp-config=<경로>)로 쓴다.  안 올리면 정본에
              서로 반대인 두 문장이 남는다 (agent-runtime:836 과 ADR-067 §2.1 이
              둘 다 ADR-034 보다 늦게 그 플래그를 지목한다)
   ADR-035    §7 「지금 열지 않는다」 -> 열렸다.  여는 조건 충족 일자와 근거(사내 실측).
              §7 말미의 「detect.go 를 탐지기 순회로」가 이 회차에서 실행됐다.
              agent-runtime §R4 앞의 MVP 값 정정 — 「MVP 는 --output-format json
              그대로」가 이 회차에서 뒤집혔다 (6절 ⑮).  R4 는 여전히 안 열렸고
              바꾼 이유는 게이트가 재는 system/init 줄이다.  ⑯ 의 type 검사도 함께
              §8 첫째 · 둘째 질문의 답 — 매칭과 주입은 가른다 ·
              자격증명의 환경변수 이름은 광고에 안 싣고 노드 로그에만 남긴다
              (canon.md 3절이 올리라고 적은 값이다)
   agent-runtime R6   「스킬 · MCP 는 자리만」 -> 구현됨.  플래그 목록 갱신
   transcript 링의 노출 정책
              stream-json 아래서 링이 사건 전문을 노드 디스크에 남긴다.
              이 팩은 그 동안 링을 닫고(6절 ⑲) 짝 팩이 선별과 함께 되살린다.
              그 선별 규칙이 짝 팩의 결정이고 정본에 올라가야 한다
   agent-runtime R3 층
              어댑터가 순수 함수만 내놓고 실행은 runner.go 가 한다는 갈래.
              6절 ⑱ 이 claude 와이어 포맷 지식을 runner.go 에 처음 올린다.
              이 팩이 Decode 를 못 만져서 생긴 이탈이고, 두 번째 하네스가 오는 날
              그 줄이 인터페이스로 올라가야 한다
   INVARIANTS · mediator-api · ADR-017
              logs/ 의 내용을 못 박는 문장 여섯 — INVARIANTS.md:257 ·
              mediator-api.md:474 · :479 · :652 · ADR-005:115 · ADR-017:286.
              앞의 둘(:474 · :115)이 「원문 그대로」라 ⑱ 에 가장 불리하다.  6절 ⑱ 이 그것에서 벗어난다.  벗어난 이유는
              SECURITY-03 준수이고, 버렸음을 표시하는 것으로 mediator-api.md:480 의
              「잘렸음을 표시한다」 규약을 따른다
   ADR-005    성질 2(봉인)의 잔여 표면.  6절 ⑰ · ⑱ 의 근거다.
              ⑱ 이 이 팩이 넓힌 몫을 닫았다 — logs/ 를 허용목록으로 거른다.
              system/init 과 최종 result 와 stderr 만 전문이고 나머지는 껍데기다.
              남는 것은 이 팩보다 먼저 있던 표면이다: 에이전트가 자격증명을 읽어
              최종 답 문자열에 스스로 실으면 그것이 봉인되고 삭제도 막힌다.
              닫으려면 봉인 기록의 편집 규칙을 이 ADR 이 열어야 한다
```

---

# 6. 회차가 실측으로 교정한 것 (2026-09-11 · 09-12)

`v3-run-harness-components` 가 팩을 오늘의 코드에 대고 확정하면서 벗어나거나 고친
자리다. ① ~ ③ 은 Requirements Analysis, ④ ~ ⑤ 는 Application Design,
**⑥ ~ ⑲ 는 2026-09-12 의 설계 검증 · 설계 질문 · 검증 루프 열한 바퀴**가 냈다. 근거 전문은
`aidlc-docs/v3-run-harness-components/inception/requirements/requirements.md` 7절과
`inception/application-design/application-design.md` 4절.

| | 항목 | 값 | 근거 |
|---|---|---|---|
| ① | 알려진 키 검증 | **이미 있다.** `internal/contract/contract.go` 의 `agentKeys` 와 `knownKeys` 가 `Contract.Validate` 에서 이미 거절하고 Mediator 는 `400` 을 낸다 (`ADR-057`). 이 회차가 할 일은 그 목록에 이름 둘(`mcp` · `pack`)을 더하는 것뿐이다 | 실측. `features.md` 3.5 와 `canon.md` 2절의 `ADR-034` §5 ④ 줄은 「이 팩이 만든다」가 아니라 「이미 있다」로 읽는다. 2절의 「`agent` 의 알려진 키」 행은 값이 맞고 범위 서술만 줄어든다 |
| ② | 팩 tar 풀기의 경로 검증 | 절대경로 항목 거부 · `..` 를 담은 항목 거부 · 심볼릭 링크 항목 거부 · 크기와 개수에 상한. `settings.json` 은 이름으로 건너뛴다. **거부는 조용하지 않다 — 그 단계를 실패로 보고한다.** 상한의 실제 값은 팩 유닛의 Functional Design 이 tar 규약과 함께 닫는다 | SECURITY-05. 팩은 계약이 적은 주소에서 받은 tar 이고 계장이 그것을 가짜 홈 **안에** 편다. 2절의 팩 형식 행은 「그 밖의 파일은 무시」만 적었고 경로를 안 적었다. `ADR-035` §3 「없음이 실패보다 나쁘다」가 여기도 같다 |
| ③ | 팩의 기대 다이제스트 | **이월.** `HarnessResult` 의 `pack: <sha256>` 기록으로 족하다. 받는 쪽이 기대값을 적는 자리(`agent.pack_sha256` 같은 것)는 두지 않는다 | SECURITY-13 이 실행 전 검증을 요구하나 근거 셋이 기록으로 족하다고 말한다 — blob 이 `0444` · `0555` 로 봉인되고(`collect.go`), 출처가 계약 argv 와 manifest 에 박히고, 명령 단계에 파일시스템 경계가 없어(`INVARIANTS`) 같은 계약이 이미 임의의 argv 를 돌린다. 팩만 더 잠그면 위협 모델은 그대로고 능력만 좁아진다. 값이 생기는 때는 팩 저장소가 여럿이 되거나 팩을 캐시할 때다 (4절) |
| ④ | 계장 디렉터리를 못 만들면 | **단계 실패다.** 하네스를 안 띄우고 사유를 보고한다. 오늘은 `os.MkdirTemp` 가 실패하면 계장을 건너뛰고 그대로 기동한다 — **이 회차가 그 동작을 바꾼다** | Application Design Q2. 디렉터리가 없으면 가짜 홈이 앉을 자리가 없고, 그대로 띄우면 개인 설정과 계정 커넥터가 다 보이는 하네스가 돈다. SECURITY-09 「가짜 홈이 기본값이다. 여는 값을 두지 않는다」와 SECURITY-15 「실패는 닫히는 쪽으로」가 정면으로 요구한다. 디스크가 찬 노드에서 조용히 격리가 풀리는 길을 막는다 |
| ⑤ | 탐지기 순회 | **세운다.** `costlyAttrs` 를 `Fingerprinter` 순회로 바꾸고 하네스 · 저장소 · MCP 셋을 그 종류로 둔다. 이름만 `Detector` 가 아니다 — `ADR-068` 의 시계가 그 이름을 쓰므로 `ADR-035` §4.2 가 든 Nomad 의 단어를 따른다 | Application Design Q3 = B. **처음에 「안 세운다」로 골랐다가 2026-09-12 검증이 뒤집었다.** 그때 근거 둘이 다 틀렸다 — 「§4.2 가 지키려던 값은 `ADR-068` 이 이미 세웠다」는 오독이고(§4.2 의 논거는 등록부이지 광고 루프가 아니다. 그 열매는 §4.3 의 `break` 풀림이다), 「이름이 부딪친다」는 다른 이름을 달면 사라진다. 「모양이 서로 다르다」도 §4.2 가 든 Nomad fingerprint 유비가 바로 그 경우를 겨눈 것이다. 결정적으로 `ADR-035:246-248` 이 「`ADR-034` 를 구현하면서 `detect.go` 를 탐지기 순회로 바꾸는 것까지는 오늘 동작을 안 바꾸고 된다」로 **이 회차를 시점으로 지목**했는데 산출물이 인용도 반박도 안 했다. **다만 권장값 그대로는 아니다** — §4.2 는 `detectors = [harnessDetector · mcpDetector · toolchainDetector]` 로 **종류 셋**을 세운다. 이 회차도 셋인데 **셋째가 다르다** — `toolchain` 이 아니라 `repo` 다. 그리고 이름을 `Detector` 에서 `Fingerprinter` 로 바꾸고 `Probe` 의 반환형도 다르다. 벗어나는 것은 개수가 아니라 **셋째의 정체와 이름과 반환형**이고, 근거는 오늘 `costlyAttrs` 가 실제로 도는 비싼 사실이 그 셋이기 때문이다 |
| ⑥ | 팩의 서버와 `agent.mcp` 필터 | **팩의 서버도 필터를 탄다.** `agent.mcp` 가 이름을 적은 것만 허용목록에 실린다. 팩이 실었으나 요청 안 한 이름은 0 이고 Notes 에 남는다 | 2026-09-12 검증. 같은 회차의 문서 셋(`features.md` 3.2 · `requirements.md` FR-2 · `services.md` 1.1)이 「팩의 서버는 전부」로, 다른 셋(`requirements.md` 4.2 SECURITY-06 · 4.3 · `application-design.md` 6.3 SECURITY-08)이 「요청한 이름만 · 요청 안 한 서버 0」으로 **정반대**를 적었고 정한 자리가 없었다. 앞이 참이면 계약 작성자가 이름을 안 적고도 임의의 stdio 서버를 물릴 수 있고 `--strict-mcp-config` 도 가짜 홈도 안 막는다 — 허용목록 자체가 싣기 때문이다. 팩은 **정의의 출처이지 허가의 출처가 아니다** |
| ⑦ | 팩이 노드 선언 이름을 덮을 때 | **거절한다.** 문구는 `pack redefines node-declared mcp server <이름>`. 팩이 새 이름을 싣는 것은 그대로 허용한다 | 2026-09-12 검증. 2절의 우선순위(팩 · 노드 · 워크스페이스)를 그대로 두면 계약이 `requires: mcp.<이름>` 으로 소유자 선언을 보고 노드를 고른 뒤 자기 팩의 정의로 그 이름을 덮어 실행할 수 있다 — **매칭은 소유자의 값으로 하고 실행은 계약의 값으로 한다.** SECURITY-08 의 「노드 선언은 소유자만 쓰는 설정 파일에 산다」가 이 경로로 무력해지고, `requirements.md` 4.3 의 오용 시나리오 넷에 이 줄이 없었다 |
| ⑧ | 훅 설정의 위치 | **가짜 홈 안이다** — `<계장 tmp>/home/settings.json`. 오늘은 `<계장 tmp>/enode-settings.json` 이다 | 2026-09-12 검증. `features.md` 3.1 이 「계장이 그 홈을 짓고 **오늘의 훅 설정을 그 안의 `settings.json` 으로 옮긴다**」이고 `constraints.md` 접점 절이 「`WriteHookSettings` 를 **가짜 홈 안으로** 옮긴다. 이 팩만」이다. 산출물 셋이 「가짜 홈 **옆**으로 · 오늘 그대로」로 적어 **근거 없이 요구를 뒤집고 있었다.** 요구대로 되돌린다 |
| ⑨ | `Instrument` 의 실패 등급 | **기본이 치명이다.** `errAux` 로 감싼 훅 설정만 보조다 (개수는 ⑪ 이 하나로 고쳤다). 그리고 보조로 삼킬 때도 이미 얻은 플래그는 붙인다 | 2026-09-12 검증. 앞선 판은 반대였고(`errComponents` 로 감싼 것만 치명) 그러면 `Instrument` 안의 오류 반환 여덟 자리 중 하나를 안 감싸는 실수가 **열리는 쪽으로** 틀린다 — 허용목록이 안 쓰였는데 `exit 0` 으로 성공이 봉인된다. 이 저장소는 같은 선택을 `env.go` 에서 이미 반대로 했고(`ADR-034` §2.1 「기본이 통과면 조용히 안 걸린다」) SECURITY-15 도 「실패는 닫히는 쪽으로」다. 플래그를 함께 돌려주는 이유는 `--strict-mcp-config` 가 격리의 **확실한 겹 하나**이기 때문이다 — `ADR-067` 은 계정 커넥터가 로그인에서 온다고 적고 FR-1 이 `.credentials.json` 을 가짜 홈에 복사하므로, 가짜 홈이 커넥터를 끊는지는 아무도 안 쟀다 |
| ⑩ | 계장 디렉터리의 보존 | **스위치를 안 둔다.** `runner.go:64` 의 `defer os.RemoveAll(tmp)` 를 그대로 둔다 | 2026-09-12 검증에서 한 번 「스위치를 둔다」로 넣었다가 **같은 검증의 다음 회차가 되돌렸다.** 3.1 의 보안 요구가 「복사한 자격증명 파일은 `0600` 이고 계장 임시 디렉터리와 함께 **단계 끝에 지워진다**」인데(`features.md` 3.1 의 보안 요구가 그 자리다 — SECURITY-12 는 사용자 인증 규칙이라 안 걸리고, SECURITY-09 · 15 도 이 값을 직접 안 받는다) 남기는 스위치가 그것을 뚫는다 — 켜면 `<tmp>/home/.credentials.json` 이 노드 디스크에 살아남고, 누가 켜는지도 언제 지우는지도 정할 자리가 없다. **대가는 눈 검증이 복제본을 잰다는 것이다** — 사람이 손으로 띄우는 경로는 환경(`harnessEnv` 가 `os.Environ()` 을 안 얹는다) · 플래그(`--settings` · `--permission-mode` · `--add-dir` 이 빠진다) · 게이트웨이 인증(`apiKeyHelper` 가 빠져 `Not logged in` 이 난다)에서 실물과 갈린다. 그 한계를 게이트가 명시로 안다 |
| ⑪ | `Instrument` 의 보조 등급 | **하나다.** 훅 설정 쓰기만 `errAux` 로 감싼다 | 2026-09-12 검증. ⑨ 를 낼 때 「훅과 기준 시각 둘」로 적었으나 **기준 시각은 구조적으로 거기 안 닿는다** — `writeStamp` 는 `runner.go:71-76` 에서 `Instrument` **앞에** 불리고 그 오류가 `== nil` 비교로 이미 버려진다. `Instrument` 안의 오류 반환은 `hook.go` 의 Marshal 과 WriteFile 둘뿐이고 그 둘이 같은 한 자리다. 「둘임을 시험이 잰다」는 완료 조건은 잴 수가 없었다 |
| ⑫ | `agent.pack` 이 미래 결합점이다 | **계약 어휘는 안 는다.** `runctl submit --pack`(4절 이월)이 생겨도 blob 을 심는 경로만 늘고 단계는 `agent.pack` 으로 그 blob 이름을 가리킨다 | 설계 질문(2026-09-12). `agent.pack` 이 blob 이름 하나만 받으므로 출처가 팩 단계든 제출 시점이든 단계 쪽 문법이 같다. 4절이 「막고 있는 값」만 적고 붙는 자리를 안 적어 다음 사람이 새 키를 만들 여지가 열려 있었다 |
| ⑬ | 가용 구성요소의 열거 면 | **`GET /v1/capabilities` 와 `runctl capabilities` 다.** `mcp.<이름>` 이 attrs 로 실리므로 자동으로 나타난다. 팩은 노드 속성이 아니라 계약이 나르는 것이라 열거에 안 나타난다 | 설계 질문(2026-09-12). 계약 작성자가 실제로 보는 면이 그것인데 이 회차의 게이트(CA2 · CA3)는 `GET /v1/nodes` 만 봤다 — 그것은 운영자 면이다. 열거가 **가용성을 안 말하는 것은 의도**다 (`ADR-014` 결정 3 · `TestCapabilitiesDoesNotLeakAvailability`) — 여유를 노출하면 check-then-act 경쟁이 생긴다. 조합 가용성은 `runctl dry-run` 이 답한다 |
| ⑮ | 하네스 출력 형식 | **이 팩이 플래그 한 줄만 가져온다** — `Argv` 에 `--output-format stream-json --verbose` 를 더한다. 나머지 출력 처리(`Decode` · tee · 사건 배출)는 짝 팩의 것으로 둔다 | 2026-09-12 사용자 결정. Q5 = A 를 **부분으로 뒤집는다.** 게이트 CA1 · CA4 · CA5 와 장면 ⑤ 가 전부 `system/init` 줄의 `mcp_servers` 를 재는데 오늘 `Argv` 가 `-p --output-format json` 이라 **그 줄이 아예 안 나온다.** 문서가 적어 둔 대안 둘이 다 안 섰다 — 「짝 팩이 바꾼다」는 병합 순서(이 팩이 먼저)와 모순이고, 「`enode` 가 남긴 프로브 출력」은 그것을 내는 유닛이 0 이었다. `scene-gates.md` 4절이 눈 검증을 보류로 안 넘긴다고 못 박았으므로 **닫을 수도 보류할 수도 없는 조각이 셋** 남아 있었다. **최종 `result` 사건은 그대로 집힌다** — `ParseClaude` 가 `lastJSONObject` 로 마지막 JSON 객체를 집는다. **그러나 크래시 경로가 뒤집힌다** — 아래 ⑯ 이 그것을 막는다. 대가 둘을 함께 적는다. 첫째, 짝 팩과의 접점이 하나에서 **둘**로 는다 (`runner.go` + `claude.go` 의 `Argv`). 둘째, **정본의 MVP 값을 R4 없이 뒤집는다** — `agent-runtime.md:327-328` 이 「MVP 는 `--output-format json` 그대로. R4 를 열 때 stream-json 으로 바꿔도 인터페이스는 안 바뀐다」로 값을 박았고 R4 는 안 열렸다 (`agent-runtime.md:479-490`). 5절 되돌리기 목록에 올린다 |
| ⑰ | 봉인 기록으로 가는 자격증명 누출 | **준수로 만든다** (⑱ 이 그 설계다). 한 번 「비준수로 적고 받아들인다」로 판정했다가 되돌렸다. `security-baseline` 의 SECURITY-03 은 「비밀·토큰·PII 가 로그에 나오면 안 된다」인데 `runner.go:155-156` 이 stdout 과 stderr 를 **원문 그대로** `logs/` 로 내고 그 묶음은 `ADR-005` 성질 2 로 봉인되어 삭제도 막힌다. 이 회차가 그것을 안 거른다 | **표면이 이 팩보다 먼저 있다** — 오늘도 에이전트가 가짜 홈의 `.credentials.json` 을 읽어 `$OUT` 이나 최종 `result` 문자열에 실을 수 있고 그것이 봉인된다. ⑮ 이 **넓힌다** — `stream-json` 아래서는 도구 입력과 도구 결과가 사건으로 함께 실린다. 거르는 장치가 필요한데 **그것이 ⑱ 이고 이 팩 안이다.** `security-baseline` 은 판정을 준수 · 비준수 · N/A 셋으로만 허용하므로(`security-baseline.md:18`) 「재판정 대기」로 둘 수 없었고, 사용자가 준수 설계를 골랐다. 사용자 결정 (2026-09-12). 되돌린 이유 — 「받아들인다」는 규칙 어휘가 아니고(`security-baseline.md:21` 이 비준수를 blocking 으로 규정한다) 에이전트가 혼자 정할 값도 아니었다. 남는 잔여는 **이 팩보다 먼저 있던 표면** 둘이다 — 에이전트가 최종 답에 스스로 싣는 것, 그리고 **명령 단계의 `logs/`**(`claim.go:647` 이 원문 그대로 올린다. ⑱ 은 하네스 단계의 `claim.go:778` 만 거른다) — 에이전트가 자격증명을 읽어 **최종 답 문자열에 스스로 실으면** 그것은 오늘도 봉인된다. 그 표면은 `ADR-005` 의 봉인 설계가 만든 것이라 5절 되돌리기로 올렸다 |
| ⑲ | 트랜스크립트 링과 ⑮ | **`stream-json` 을 켜는 동안 하네스 단계의 링 tee 를 끈다** — `runner.go:103-105` **하나**다. U1 이 그 줄을 끄고 짝 팩이 선별과 함께 되살린다. **명령 단계의 tee(`claim.go:632-635`)는 그대로 흐른다** — 링에 기록자가 둘이고 ⑲ 가 끄는 것은 하나뿐이다 | ⑱ 이 `logs/` 만 걸러서는 **안 닫힌다.** `runner.go:104-105` 의 `io.MultiWriter(&stdout, j.Transcript)` 가 ⑱ 의 선별 자리(`:155-156`)보다 **앞**이고, `claim.go:370-371` 이 설정만 있으면 링을 **언제나** 열며 끄는 스위치가 없다. 링은 512 KiB 파일로 **노드 디스크에 남는다**(`transcript.go:28`). 그래서 ⑮ 이 여는 누출이 그 문으로 그대로 나가고 ⑰ 의 준수가 무너진다. **대가는 0 이 아니다.** 기준을 ⑮ 이전 세계에 대고 재면 0 처럼 보인다 — 오늘 `Argv` 가 `-p --output-format json` 이라 하네스 단계에서 링에 가는 것이 끝의 봉투 하나뿐이다. 그러나 **⑮ 이 바로 그 `Argv` 를 바꾼다.** ⑮ 이후라면 링에 사건 전부가 흘러 `requirements/enode-features.md` 3.1.3(「노드 소유자가 에이전트가 지금 뱉는 글자를 자기 제어판에서 본다」)과 그 게이트 CP6 ① 이 **공짜로 초록이 될 수 있었다.** ⑲ 가 그것을 짝 팩까지 **미룬다** — 그 사건 전문이 곧 누출이기 때문이다. 명령 단계 화면은 안 죽는다. **링을 쓸모 있게 만드는 것이 짝 팩의 일이고**, 그 팩이 사건 스트림을 나르면서 노출 정책을 함께 정한다 — 이 팩이 그 자리를 반쯤 열어 두고 넘기지 않는다. 사용자 결정 「준수 설계로 만든다」가 이 길을 가리킨다 (B — 링에도 같은 선별 — 은 필터가 실시간으로 돌아야 해서 exec 뒤 한 번에 거르는 ⑱ 과 구조가 갈린다). **넷째 길이 있었고 이번에 안 골랐다** — `Job.Transcript` 가 이미 `io.Writer` 라(`runner.go:55`) 거기에 ⑱ 의 허용목록을 줄 단위로 도는 래퍼를 끼우면 보안과 3.1.3 을 **둘 다** 얻는다. 비용은 `bufio.Scanner` 한 겹이고 B 를 기각한 근거(「실시간이라 구조가 갈린다」)는 그 비용을 **측정 없이** 쓴 것이다. 짝 팩이 링을 되살릴 때 이 길을 먼저 본다 |
| ⑱ | `logs/` 에 싣는 것 | **도구 사건은 껍데기만 남긴다** (사용자 결정 2026-09-12). 경로에 예외가 없다.

**경계는 허용목록이다** — 아는 것만 남기고 나머지는 걷는다. 이 저장소가 `env.go` 의
환경변수에서 이미 고른 규율이고(`agent-runtime` R1), 모르는 사건이 생겼을 때
**닫히는 쪽으로 틀린다**.

```text
   남긴다 (전문)   system/init 사건 · 최종 result 사건 · stderr
   남긴다 (껍데기)  그 밖의 모든 사건 — 사건 종류 · 도구 이름 · 성공 여부. **시각은 안 넣는다** — `runner.go` 가 exec 이 끝난 뒤 한 번에 선별하므로 사건마다의 시각을 못 찍고, 넣으면 모든 줄이 같은 값이라 정보량이 0 이다.
                   본문은 어느 사건에서도 안 남긴다.  assistant 의 text 도,
                   thinking 도, 도구 결과도 같다
   표시한다        걷었음을 한 줄로 남긴다.  그 줄은 첫 줄이 아니다 —
                   system/init 이 나오는 경로에서 첫 줄은 언제나 그것이다
                   (게이트가 head -1 로 읽는다).  init 이 아예 안 나오는 경로
                   (플래그 오류 · 기동 실패)에서는 stderr 가 첫 줄이고
                   그 단계는 harness_error 다 (⑯)
```

**「도구 사건」이라는 사건 종류는 `stream-json` 에 없다.** 그래서 「도구 사건만
걷는다」로 적으면 구현자가 `assistant` · `user` 를 어느 쪽으로 볼지 동전을 던진다.
위 허용목록은 그 물음을 없앤다 — **둘만 전문이고 나머지는 전부 껍데기다.**

**실물을 아직 못 봤다.** 저장소에 `stream-json` 픽스처가 0 이고
`harness.go:212-214` 가 「종류는 stream-json 을 켤 때 실물을 보고 늘린다」로 모른다고
명시한다. 그래서 **U1 이 실측하고 사건 종류 목록을 `decisions.md` 에 행으로 적는
것까지가 그 유닛의 일이다.** 목록이 늘어도 허용목록이라 기본 동작은 안 바뀐다

**한 번 「봉투가 안 나오는 경로에서는 stdout 을 그대로 싣는다」로 예외를 뒀다가 걷었다** — 그 경로는 드문 예외가 아니다. `runner.go:149-151` 이 `ctx.Err()` 로 임대 만료를 덮고 그때 봉투가 안 나오는데, 임대 만료는 긴 에이전트 단계의 일상이고 크래시는 ⑯ 이 존재하는 이유다. 예외를 두면 ⑱ 이 닫으려던 「읽기만 해도 샌다」가 그 경로로 되열리고 ⑰ 의 준수가 무너진다.

**껍데기를 남기는 것이 이 팩이 고를 수 있는 것 중 자기충족 손실이 가장 작다.** 다만 정직하게 적는다 — `ADR-005:52-53` 은 `logs/` 가 아니라 `ADR-004` 의 기계적 성공 조건에 대한 문장이고 그 대조 재료(`steps/NN-*.json` · `verdict.json`)를 ⑱ 이 안 건드리므로 **그 조항은 애초에 안 위협받는다.** 그리고 `ADR-017:286` 의 「무엇을 읽었는지」는 **안 산다** — 도구 이름 `Read` 는 파일마다 같은 글자라 `:285` 가 Record 몫으로 떼어 둔 좌표보다 거칠다. **잃는 것을 잃는다고 적고 5절에 올린다.**

**선별은 `runner.go` 가 직접 한다.** 진짜 이유는 **이 팩이 `Decode` 를 못 만지기 때문**이다 — `constraints.md` 접점 절이 `Decode` 를 짝 팩에 배정했고 `unit-of-work-file-matrix.md` 가 「`Decode` 는 어느 유닛도 안 만진다」로 받는다. 어댑터 쪽 자리가 막혀 있다.

**이것은 R3 층의 이탈이다.** `agent-runtime` R6 는 유예하는 동안 그 일을 「`Instrument` 가 들고 있다」로 어댑터에 두라고 적고, `components.md` 가 「어댑터가 순수 함수만 내놓고 실행은 `runner.go` 가 한다는 갈래는 안 바뀐다」를 단언한다. ⑱ 은 claude 와이어 포맷 지식을 `runner.go` 에 **처음** 올린다(오늘 그 파일에 `encoding/json` import 가 없다). 같은 회차의 Q1 이 정반대 저울을 썼다 — 「`runner.go` 가 `CLAUDE_CONFIG_DIR` 이라는 claude 전용 이름을 알게 된다」를 기각 근거로 삼아 인터페이스를 바꿨다. **5절 되돌리기에 올린다.** 두 번째 하네스가 오는 날 이 줄이 인터페이스로 올라간다.

**「최종 `result` 봉투」를 `lastJSONObject` 로 뽑으면 안 된다** — 크래시 때 마지막 완결 객체가 `assistant` 사건이라 ⑱ 이 닫으려던 길이 다시 열린다. `type == "result"` 로 고른다 (⑯ 과 같은 검사다) | ⑰ 을 준수로 만드는 설계다. **⑮ 이 넓힌 것은 하네스의 출력이 아니라 `logs/` 의 내용**이고, 둘은 `runner.go:155-156` 에서 갈린다 — 거기서 stdout 전체를 원문으로 낸다. 하네스는 `stream-json` 으로 돌려 `init` 줄을 얻고(게이트의 값), `logs/` 에는 오늘과 같은 것에 `init` 줄과 도구 사건의 껍데기가 더해진다. **⑲ 와 함께여야 ⑮ 이 넓힌 표면이 0 이 된다** — `logs/` 만 걸러서는 링이 남는다 — 에이전트가 파일을 *읽기만* 해도 새던 길이 닫히고, 오늘처럼 *스스로 말해야* 남는다. 전체 스트림이 필요한 쪽은 짝 팩이다. **한 번 「그 목적지가 링이라 분업선이 선명해진다」로 적었는데 그것이 곧 「누출이 그리로 간다」는 뜻이었다** — ⑲ 가 그 자리를 닫는다. **정본에서 벗어난다** — `INVARIANTS.md:257`(「로그의 소유자 — Record 안에 원문을 옮긴다. 참조만 하면 성질 4 가 깨진다」) · `mediator-api.md:479`(「log — 그 단계가 뱉은 것」) · `:652` · `ADR-017:286`(「무엇을 읽었는지는 `logs/` 에 남는다」)이 `logs/` 의 **내용**을 못 박는다. 특히 `ADR-017` 은 기록의 자기충족이 약해지는 것을 `logs/` 로 상계했는데 ⑱ 이 지우는 것이 정확히 그 「무엇을 읽었는지」다. **⑮ 처럼 5절 되돌리기에 올린다.** `mediator-api.md:480` 의 「잘렸음을 표시한다」는 **크기 절단**이고 ⑱ 은 **내용 선별**이라 종류가 다르다. 표시한다는 규약만 따른다 — 정본이 내용 선별을 허용한 문장은 없고 `ADR-005:115` 와 `mediator-api.md:474` 의 「**원문 그대로**」가 오히려 반대다 |
| ⑯ | 봉투의 `type` 검사 | **`ParseClaude` 가 switch **앞에서** `type` 을 본다.** `"result"` 가 아니면 `ReasonError` 다. **`ReasonError` 로 떨어질 때도 `Turns` · `CostUSD` · `Session` 은 채운다** — 오늘 `harness_test.go:64-69` 의 `TestHarnessRecordsBudget` 이 `type` 없는 유일한 픽스처인데 거기서 재는 것은 `Turns` · `CostUSD` **둘**이다(`Session` 은 `:72-82` 가 재고 그 픽스처는 `type:result` 라 이 분기를 안 탄다). 그래서 셋을 값으로 박는다 — 시험이 `Session` 을 안 재므로 안 박으면 빠뜨려도 초록이다. 둘만 보면, 조기 반환으로 짜면 빨갛고 `h` 를 세운 뒤 판정만 바꾸면 초록이다. 구현자가 동전을 던지지 않게 값으로 박는다. **switch 뒤에 두면 안 된다** — 둘째 case 가 `subtype` 에 `token` 이 들어가면 무엇이든 `ReasonMaxTokens` 로 떨어뜨려 `Completed()` 가 참이 되고 `$OUT` 이 수확된다. **`Message` 에는 봉투의 `type` 만 싣는다 — 원문 줄을 안 싣는다.** 그 분기의 입력이 크래시 때의 `{"type":"assistant",...}` 이고 그것은 ⑱ 이 `logs/` 에서 본문을 지우기로 한 바로 그 객체다. `Message` 는 `claim.go:784` 의 `res.Error` 와 `harness.go:57` 의 `steps/NN-*.json` 으로 **봉인에 들어간다** — 원문을 넣으면 ⑱ 이 닫은 길이 뒷문으로 열린다. U1 이 그 줄과 시험을 진다 | ⑮ 이 열었고 4차 검증이 찾았다. `harness.go:131-143` 의 `lastJSONObject` 는 **줄 단위가 아니라 출력 끝에서 유효한 마지막 JSON 객체**를 집는다. `stream-json` 아래서 하네스가 중간에 죽으면 그것이 `{"type":"assistant",...}` 이고, `harness.go:111-121` 의 switch 는 `Subtype` 과 `IsError` 만 보므로 `default: ReasonOK` 로 떨어진다. `claudeEnvelope.Type` 은 `harness.go:88` 에 선언돼 있고 **파일 전체에서 읽는 자리가 0** 이다. 그래서 **크래시가 성공으로 봉인된다** — `Completed()` 가 참이라 `$OUT` 이 수확되고 남는 서명은 `reason=ok · turns=0 · cost_usd=0` 이다. `harness.go:98-99` 의 주석이 「봉투가 아예 안 나오면(크래시·플래그 오류) `harness_error` 다 — 종료코드 0 을 믿지 않는다」로 그 보증을 명문화했는데 ⑮ 가 그것을 무효로 만든다. **같은 회차의 ⑨ 가 「실수는 닫히는 쪽으로 틀려야 한다」로 세운 원칙을 정면으로 어기는 자리다** (SECURITY-15). 오늘 코드에도 있어야 할 검사다 — `Type` 을 선언만 하고 안 읽는 것 자체가 결함이다 |
| ⑭ | `mcpUp` 이 못 잡는 것 | **셋을 명시로 적는다** — 틀린 자격증명 · 죽은 엔드포인트 · 실행은 되나 MCP 로 답 안 하는 바이너리. 「뜨나」는 존재이지 동작이 아니다 | 설계 질문(2026-09-12). 설계는 이미 알고 있었다 — `scene-gates.md` 가 가짜 서버 `true` 를 쓰면서 「`mcp_servers` 에 `failed` 로 나타나며 그 이름이 **목록에 있다는 것**이 판정 재료」라고 적는다. 그런데 그 한계가 `features.md` 3.3 이나 `components.md` 2.2 에 문장으로 없어서 다음 사람이 「검증됐다」로 읽는다. 깊은 검증(HTTP 접근 · stdio 구동)을 안 하는 근거는 `requirements.md` 4.4 의 순연이다 — 5분마다 전 노드가 남의 서버를 두드리면 그 서버가 죽을 때 함대가 함께 죽는다 |

---

## 6.1 뒤집힌 값의 문자열 — 세는 것으로 지킨다

`CONVENTIONS.md` 1.4 가 「**세는 것으로 지킨다. 도구는 두 벌로 두지 않는다**」다.
6절이 뒤집은 값마다 **옛 문자열**을 여기 적고 저장소에서 센다. 손과 기억으로
도는 「전수 grep 했다」는 검증할 수 없는 주장이고, 실제로 4차 검증이 **같은 hunk
안에 있던 줄**도 놓친 것을 찾았다.

**첫 판이 거짓 초록을 냈다** (5차 검증). 열한 열쇠 중 둘이 **저장소 역사에 존재한
적 없는 문자열**이었다 — 백틱이 빠져서다. `git log -S` 가 그 열쇠를 세운 커밋
하나만 낸다. **손과 기억을 없애려고 만든 장치가 손과 기억으로 쓰였다.**

그래서 규칙을 셋 세운다.

```text
   ①  열쇠를 지어내지 않는다      뒤집기 직전 판에서 실제로 나오던 문자열을 쓴다.
                                git log -S'<열쇠>' 가 두 커밋 이상(생김 · 걷힘)을
                                내야 그 열쇠가 실재했다는 뜻이다
   ②  어형 변이를 함께 적는다      같은 값이 여러 어형으로 흩어진다.
                                열쇠 칸에 어형을 모두 적고 하나라도 남으면 빨갛다
   ③  집행자는 사람이다           도는 스크립트가 아직 없다.  기계로 적으면 그것이
                                또 거짓 초록이다.  진행자가 유닛 병합 전에 돈다
```

| 결정 | 세는 문자열 (어형 포함) | 기대값 | 예외로 남는 자리 |
|---|---|---|---|
| ⑤ | `Detector 인터페이스를 안 세운다` | 0 | 없다 |
| ⑤ | `mcpAttrs` | 기록 자리만 | 계획 둘의 옛 본문 · 옛 이름 병기 |
| ⑥ | `mcp.json 에 적힌 것 전부` | 0 | 없다 |
| ⑧ | `enode-settings.json` | 기록 자리만 | 「오늘은 여기다」로 대비를 적는 자리 |
| ⑧ | `가짜 홈 옆` | 1 | `application-design.md` 7절 ⑧ — 뒤집힌 사실을 적는 자리 |
| ⑨ | `errComponents` | 기록 자리만 | 계획 둘 · 「처음 설계는 반대였고」 |
| ⑪ | `기준 시각 파일` 이 보조 등급 표에 있는 것 | 0 | `components.md` 의 「(이 표 밖)」 행 — 뒤집었음을 적는 자리다 |
| ⑮ | 겹침을 하나로 세는 어형 — `겹치는 것은 ...하나` (백틱 유무) · `접점 — 하나` · `접점이 여섯에서 하나로 준다` | 0 | `audit.md` (역사다). **`겹치는 파일은` 은 열쇠가 아니다** — 고쳐진 본문(`겹치는 파일은 둘이다`)에도 걸려 거짓 빨강을 낸다 |
| ⑮ | 사람이 손으로 띄우는 경로 — `사람 경로`, `사람이 claude 를 한 번 직접 띄워`, `사람이 직접 띄워서` | 0 | 개정 각주가 붙은 자리 — 계획 둘과 `requirement-verification-questions.md` |
| ⑯ | `봉투가 안 흔들린다` · `봉투 파싱이 안 흔들린다` | 0 | 계획의 Q5 기록 자리 |
| ⑮ | 접점을 **셋**으로 세는 것 — `claude.go · runner.go · hook.go` 를 나란히 적는 자리 | 0 | 없다. 정본은 둘이다 — `hook.go` 는 「이 팩만」이라 접점이 아니다 |
| ⑰ | SECURITY-03 을 「비준수」로 적은 자리 | 0 | 없다. ⑱ 이 준수로 만들었다 |
| ⑱ | `둘만 싣는다` · `stderr 는 오늘 그대로` · `봉투가 안 나오는 경로` (백틱 없이 센다) | 0 | `decisions.md` 6절 ⑱ 의 「한 번 예외를 뒀다가 걷었다」 문장 — 뒤집었음을 적는 자리다 |
| ⑱ | `도구 사건의 내용을 걷는다` · `도구 사건만` | 0 | 반박 인용 자리 — 6절 ⑱ 의 「그렇게 적으면 동전을 던진다」 |
| ⑱ | `둘만 전문` · `만 전문이다` | 0 | 전문은 셋이다 — init · 최종 result · stderr |
| ⑲ | `tee 는 안 건드린다` · `tee 와 사건 배출` · `tee 는 짝 팩의 것` | 0 | 없다. 하네스 단계의 tee 는 이 팩이 끈다 |
| ⑲ | `분업선이 오히려 선명해진다` | 0 | 뒤집었음을 적는 자리 — 6절 ⑱ |
| ⑲ | `링을 닫는다` (범위 없이) | 0 | 끄는 것은 하네스 단계의 tee 하나다 |
| — | SECURITY-12 를 계장 자격증명 복사의 근거로 든 자리 | 0 | 없다. 그 규칙은 사용자 인증이다 |
| — | 6.1 표를 **CA0 의 기계 검사**로 적은 자리 | 0 | 없다. 집행자는 사람이다 |
| — | `grep -c 'mux.HandleFunc' internal/api/api.go` | 기각 근거 자리만 | 「이 명령은 17 을 낸다」로 반박하는 자리 |

### 검사 범위와 예외

표를 그대로 `grep` 하면 **표 자신이 모든 행의 히트**이고 제품 코드와 다른 회차
문서까지 번진다. 범위를 먼저 못 박는다.

```text
   센다        aidlc-docs/v3-run-harness-components/inception/**
               requirements/harness-components/**   (이 6.1 절 자신은 뺀다)

   안 센다     audit.md 와 aidlc-state.md 의 경위 서술   역사다.  고치면 기록이 아니다
               inception/plans/**                       질문과 답의 기록.  개정 각주로 대신한다
               internal/** · README.md                  제품 코드와 저장소 설명.  이 표의 대상이 아니다
               aidlc-docs/<다른 회차>/ · enode-design/   이 회차가 못 고친다
```

**진행자가 유닛 병합 전에 이 표를 돈다.** CA0 의 기계 검사에 안 넣는다 — 도는
스크립트가 없는 채로 기계라고 적으면 그것이 다시 거짓 초록이다. `scene-gates.md`
CA0 이 이 표를 돈다고 적었던 줄도 함께 걷었다.

**오래 가는 답은 스크립트다.** 정규식과 `file:line` 허용 목록으로 세고 허용 목록
밖의 히트가 하나라도 있으면 빨갛다. 자리는 이 저장소의 `scripts/` 다
(`glyphscan.go` 옆). 짓는 것은 Construction 의 몫이라 여기서는 자리만 적는다.

---

**계장 임시 디렉터리는 새로 만들지 않는다.** `runner.go` 가 이미
`os.MkdirTemp("", "enode-inst-*")` 로 짓고 `defer os.RemoveAll` 로 단계 끝에
지운다. 가짜 홈과 허용목록과 팩이 그 아래 살고 같은 `defer` 가 함께 지운다 —
새 정리 코드가 0 이다.

`Fixed()` 가 인자를 안 받아 그 경로를 모르던 자리는 Application Design 이
닫았다 (D1) — **`Fixed(dir string) map[string]string` 으로 인터페이스를 바꾼다.**
`runHarness` 가 홈 변수를 직접 합치면 그 파일이 `CLAUDE_CONFIG_DIR` 이라는
claude 전용 이름을 알게 되고, `Instrument` 가 env 까지 돌려주면 계장이 실패할 때
홈이 안 갈려 2절의 「계장 실패와 독립」이 깨진다. 두 번째 하네스가 없어 이주
비용은 0 이다.
